#autosaving_indication { display: inline-block; width: 22px; height: 20px; }
.saved { background-image: url("/images/status.png"); background-position: 0px 0px; }
.not-saved { background-image: url("/images/status.png"); background-position: 0px 24px; }

textarea#journaltext,div#uneditable_entry_form,textarea{font-size: 18px;}

textarea:focus{border:0px}
#journaltext{resize:vertical;width:100%;background: rgb(255, 255, 255) transparent; /* default fallback */}
fieldset:focus{border: 0px}
textarea{border:0px}
textarea{font-size: 18 px;}
h1#date{color: #400606;}



/* Sticky footer styles
-------------------------------------------------- */

html,
body {
  height: 100%;
  /* The html and body elements cannot have any padding or margin. */
}

/* Wrapper for page content to push down footer */
#wrap {
  min-height: 100%;
  height: auto !important;
  height: 100%;
  /* Negative indent footer by its height */
  margin: 0 auto -60px;
  /* Pad bottom by footer height */
  padding: 0 0 60px;
}

/* Set the fixed height of the footer here */
#footer {
  height: 60px;
  background-color: #f5f5f5;
}

textarea:focus, input:focus{
    outline: 0;
}
